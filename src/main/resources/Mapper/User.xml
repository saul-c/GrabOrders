<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.lightina.GrabOrders.dao.UserMapper">
    <!-- 缓存类配置 -->
    <cache type="cn.lightina.GrabOrders.redis.RedisCache" />

    <select id="getUserById" parameterType="int" resultType="User" useCache="true">
        select userid userId,username userName,passwd passWd from user where userId = #{userId}
    </select>

    <insert id="addUser" parameterType="User">
        insert into user (userid,username,passwd) values(null,#{userName},#{passWd})
    </insert>

    <select id="checkLogin" parameterType="User" resultType="User">
        select * from user_info where userId=#{user.user_id} and passWd=#{user.passWd};
    </select>
</mapper>